version: '3'

services:

  website:
    build: .
    command: >
      gunicorn -c "python:config.gunicorn" --reload "ensimpl.app:create_app()"
    env_file:
      - '.env'
    volumes:
      - '.:/app/ensimpl'
    ports:
      - '9000:9000'
